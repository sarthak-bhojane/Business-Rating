1. Convert zip file and Place project inside htdocs
2. Open Xammp - Start apache and mysql
3. goto this url - http://localhost/phpmyadmin
4. Import database.sql code inside http://localhost/phpmyadmin/ for that click on new in side pannel and give db name business_rating after that click on create button. open sql tab, inside empty text-area paste database.sql file code, enter go button.
5. Update config/db.php [NOTE:if db name business_rating you mention different then change this db.php otherwise remain as it is.]
6. Access: http://localhost/business-rating